[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[project]
name = "podcast-pipeline-tool"
version = "1.0.0"
description = "Podcast Pipeline Tool"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Proprietary"}
